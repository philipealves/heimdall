language: java
install: true
sudo: required

jdk:
  - oraclejdk8
os:
  - linux
branches:
  only:
    - master
cache:
  directories:
    - $HOME/.gradle

script:
  - ./gradlew check
  - ./gradlew jacocoTestReport

after_success:
  - bash <(curl -s https://codecov.io/bash)